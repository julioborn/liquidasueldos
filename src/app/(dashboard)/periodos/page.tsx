import { PlaceholderModulo } from "@/components/placeholder-modulo";

export default function PeriodosPage() {
  return (
    <PlaceholderModulo
      titulo="Períodos y novedades"
      descripcion="Carga de variables del mes (días, horas extra, inasistencias, licencias) y selección de empresa/período a liquidar."
    />
  );
}
