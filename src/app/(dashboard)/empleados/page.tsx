import { PlaceholderModulo } from "@/components/placeholder-modulo";

export default function EmpleadosPage() {
  return (
    <PlaceholderModulo
      titulo="Empleados"
      descripcion="Legajo, datos personales, CUIL, fecha de ingreso, categoría, convenio, datos bancarios."
    />
  );
}
