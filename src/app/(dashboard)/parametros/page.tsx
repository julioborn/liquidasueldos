import { PlaceholderModulo } from "@/components/placeholder-modulo";

export default function ParametrosPage() {
  return (
    <PlaceholderModulo
      titulo="Parámetros normativos"
      descripcion="ABM de las tablas con vigencia por fecha: escalas, alícuotas, topes SIPA, asignaciones familiares, escala de Ganancias."
    />
  );
}
